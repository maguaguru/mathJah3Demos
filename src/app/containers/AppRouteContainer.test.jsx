/* globals expect, describe, it */

import { AppRouteContainer } from '.'

describe('App Route Container', () => {

    it('should be defined', () => {
        expect(AppRouteContainer).toBeDefined()
    })
})